name: Junit Step Annotate
description: Annotates your build step using JUnit XML reports
author: https://github.com/queimadus
requirements:
  - docker
configuration:
  properties:
    reports-paths:
      type: string
      type: array
  required:
    - reports-paths
  additionalProperties: false
